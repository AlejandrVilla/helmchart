apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "villaChart.fullname" . }}-test-connection"
  labels:
    {{- include "villaChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "villaChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
